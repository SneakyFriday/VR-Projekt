using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SettingsChangeEvent : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Hello Lea");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
